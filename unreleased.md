### Added
- Option to delete WebAuthn passkeys from the Settings page.
- Secure "Reveal" feature for sensitive settings (Home Assistant Token, Cloudflare Token, etc.) requiring password re-authentication.

### Changed
